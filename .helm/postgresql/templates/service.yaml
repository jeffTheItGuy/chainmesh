apiVersion: v1
kind: Service
metadata:
  name: chainmesh-postgresql-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: chainmesh-postgresql
spec:
  type: {{ .Values.service.type }}
  selector:
    app: chainmesh-postgresql
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: postgres
