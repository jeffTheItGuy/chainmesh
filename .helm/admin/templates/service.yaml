apiVersion: v1
kind: Service
metadata:
  name: chainmesh-admin-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: chainmesh-admin
spec:
  type: {{ .Values.service.type }}
  selector:
    app: chainmesh-admin
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
