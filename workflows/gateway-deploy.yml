name: Gateway Deploy
on:
  workflow_dispatch:
env:
  NAMESPACE: chainmesh
  DOMAIN: api.chainmesh.nimbusurf.com
  RELEASE_NAME: gateway-release
  DEPLOYMENT_NAME: chainmesh-gateway
  SERVICE_PORT: 8080
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Setup SSH and Copy Files to Remote
        env:
          REMOTE_SERVER: ${{ secrets.REMOTE_SERVER }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          REMOTE_WORKSPACE: ${{ secrets.REMOTE_DEPLOY_DIR }}/gateway-${{ github.run_id }}
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          ssh-keyscan -H "$REMOTE_SERVER" >> ~/.ssh/known_hosts 2>/dev/null || true
          ssh -o StrictHostKeyChecking=yes "$REMOTE_USER@$REMOTE_SERVER" "rm -rf $REMOTE_WORKSPACE && mkdir -p $REMOTE_WORKSPACE/.helm && mkdir -p $REMOTE_WORKSPACE/backend"
          scp -o StrictHostKeyChecking=yes -r .helm/gateway "$REMOTE_USER@$REMOTE_SERVER:$REMOTE_WORKSPACE/.helm/"
          scp -o StrictHostKeyChecking=yes -r backend/* "$REMOTE_USER@$REMOTE_SERVER:$REMOTE_WORKSPACE/backend/"
      - name: Build and Deploy on Remote Server
        env:
          REMOTE_SERVER: ${{ secrets.REMOTE_SERVER }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          REMOTE_WORKSPACE: ${{ secrets.REMOTE_DEPLOY_DIR }}/gateway-${{ github.run_id }}
          BUILD_ID: ${{ github.run_id }}-${{ github.run_number }}
          RESTART_REASON: "github-actions-deploy"
        run: |
          ssh -o StrictHostKeyChecking=yes "$REMOTE_USER@$REMOTE_SERVER" \
            RESTART_REASON="$RESTART_REASON" \
            BUILD_ID="$BUILD_ID" \
            REMOTE_WORKSPACE="$REMOTE_WORKSPACE" \
            NAMESPACE="${{ env.NAMESPACE }}" \
            DOMAIN="${{ env.DOMAIN }}" \
            DEPLOYMENT_NAME="${{ env.DEPLOYMENT_NAME }}" \
            RELEASE_NAME="${{ env.RELEASE_NAME }}" \
            SERVICE_PORT="${{ env.SERVICE_PORT }}" \
            bash << 'ENDSSH'
          set -e
          export KUBECONFIG=/etc/rancher/k3s/k3s.yaml
          cd "$REMOTE_WORKSPACE"
          docker system prune -f
          docker builder prune -f
          docker build --no-cache -f backend/gateway/Dockerfile -t chainmesh-gateway:$BUILD_ID backend/
          docker save chainmesh-gateway:$BUILD_ID | k3s ctr images import -
          TIMESTAMP=$(date +%s)
          helm upgrade --install $RELEASE_NAME .helm/gateway \
            --namespace "$NAMESPACE" \
            --set namespace="$NAMESPACE" \
            --set ingress.host="${DOMAIN}" \
            --set restartTimestamp="$TIMESTAMP" \
            --set restartReason="$RESTART_REASON" \
            --set image.repository="chainmesh-gateway" \
            --set image.tag="$BUILD_ID" \
            --set image.pullPolicy="Never" \
            --timeout=600s
          kubectl wait --for=condition=available --timeout=300s deployment/$DEPLOYMENT_NAME -n $NAMESPACE || (kubectl logs -n $NAMESPACE -l app=$DEPLOYMENT_NAME --tail=50 && helm rollback $RELEASE_NAME -n $NAMESPACE && exit 1)
          ENDSSH
      - name: Cleanup Remote Workspace
        if: always()
        run: |
          ssh -o StrictHostKeyChecking=yes "${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_SERVER }}" "rm -rf ${{ secrets.REMOTE_DEPLOY_DIR }}/gateway-${{ github.run_id }}"
