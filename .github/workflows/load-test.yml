name: Load Test (in-cluster)

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  NAMESPACE: chainmesh

jobs:
  load:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        env:
          REMOTE_SSH: ${{ secrets.REMOTE_SSH }}
          REMOTE_SERVER: ${{ secrets.REMOTE_SERVER }}
        run: |
          set -euo pipefail

          if [ -z "${REMOTE_SSH:-}" ]; then
            echo "REMOTE_SSH secret is missing." >&2
            exit 1
          fi

          if [ -z "${REMOTE_SERVER:-}" ]; then
            echo "REMOTE_SERVER secret is missing." >&2
            exit 1
          fi

          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

          printf '%s\n' "$REMOTE_SSH" | tr -d '\r' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          ssh-keyscan -H "$REMOTE_SERVER" >> ~/.ssh/known_hosts 2>/dev/null || true
          chmod 644 ~/.ssh/known_hosts

      - name: Copy k6 script to k3s node
        env:
          REMOTE_SERVER: ${{ secrets.REMOTE_SERVER }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
        run: |
          set -euo pipefail

          : "${REMOTE_SERVER:?REMOTE_SERVER secret is missing}"
          : "${REMOTE_USER:?REMOTE_USER secret is missing}"

          ssh -o BatchMode=yes -o StrictHostKeyChecking=yes "$REMOTE_USER@$REMOTE_SERVER" \
            "mkdir -p /tmp/chainmesh-load"

          scp -o BatchMode=yes -o StrictHostKeyChecking=yes tests/load/gateway_load.js \
            "$REMOTE_USER@$REMOTE_SERVER:/tmp/chainmesh-load/gateway_load.js"

      - name: Install k6 and run load test inside k3s
        id: load
        continue-on-error: true
        env:
          REMOTE_SERVER: ${{ secrets.REMOTE_SERVER }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          TEST_API_KEY: ${{ secrets.PROD_TEST_API_KEY }}
        run: |
          set -euo pipefail

          : "${REMOTE_SERVER:?REMOTE_SERVER secret is missing}"
          : "${REMOTE_USER:?REMOTE_USER secret is missing}"
          : "${TEST_API_KEY:?PROD_TEST_API_KEY secret is missing}"

          ssh -o BatchMode=yes -o StrictHostKeyChecking=yes "$REMOTE_USER@$REMOTE_SERVER" \
            NAMESPACE="$NAMESPACE" \
            TEST_API_KEY="$TEST_API_KEY" \
            bash << 'ENDSSH'
          set -euo pipefail

          export KUBECONFIG=/etc/rancher/k3s/k3s.yaml

          if [ ! -x /tmp/k6 ]; then
            curl -fsSL https://github.com/grafana/k6/releases/download/v0.54.0/k6-v0.54.0-linux-amd64.tar.gz -o /tmp/k6.tgz
            tar -xzf /tmp/k6.tgz -C /tmp
            mv /tmp/k6-v0.54.0-linux-amd64/k6 /tmp/k6
            chmod +x /tmp/k6
          fi

          GW_SVC=$(kubectl get svc -n "$NAMESPACE" -o name | grep -i gateway | head -1 | sed 's|service/||')
          GW_IP=$(kubectl get svc "$GW_SVC" -n "$NAMESPACE" -o jsonpath='{.spec.clusterIP}')
          GW_PORT=$(kubectl get svc "$GW_SVC" -n "$NAMESPACE" -o jsonpath='{.spec.ports[0].port}')

          GATEWAY_URL="http://${GW_IP}:${GW_PORT}/v1/"

          echo "Load-testing: $GATEWAY_URL"

          # Change directory so k6 writes load-summary.md here
          cd /tmp/chainmesh-load
          
          /tmp/k6 run \
            --env GATEWAY_URL="$GATEWAY_URL" \
            --env TEST_API_KEY="$TEST_API_KEY" \
            gateway_load.js
          ENDSSH

      - name: Fetch results from k3s node
        if: always()
        env:
          REMOTE_SERVER: ${{ secrets.REMOTE_SERVER }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
        run: |
          set -euo pipefail

          # Fetch the markdown summary generated by k6's handleSummary()
          scp -o BatchMode=yes -o StrictHostKeyChecking=yes \
            "$REMOTE_USER@$REMOTE_SERVER:/tmp/chainmesh-load/load-summary.md" ./load-results.md \
            || echo "::warning::Could not fetch load results"

      - name: Publish results to job summary
        if: always()
        run: |
          if [ -f load-results.md ]; then
            # Append the markdown directly to the UI
            cat load-results.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "No load results were produced." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Fail job if thresholds exceeded
        if: steps.load.outcome == 'failure'
        run: |
          echo "::error::Load test failed thresholds — see the job summary above."
          exit 1